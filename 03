import pandas as pd

data = {
    'Name': ['Alice', 'Bob', 'Charlie', 'Diana', 'Ethan', 'Fiona'],
    'Gender': ['F', 'M', 'M', 'F', 'M', 'F'],
    'Age': [25, 30, 35, 28, 40, 22]
}

df = pd.DataFrame(data)
print(df)

#Q. 성별(Gender)별 평균 나이(Age)를 계산하시오.
df.groupby('Gender')['Age'].mean()
